name: build-&-test
 
on:
  push: 

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: build
        run: |
         cd Res/
         make test
         ./test
          
